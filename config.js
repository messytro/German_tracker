// Reuse the SAME Supabase project you already set up for the day planner —
// paste the exact same URL and anon key from that project's config.js here.
// (You just need one extra table in that same project — see README.md.)
//
// This is a plain script (not a module) on purpose, so index.html's existing
// classic <script> tag can read these as window globals without needing to
// restructure the whole file into an ES module.

window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
